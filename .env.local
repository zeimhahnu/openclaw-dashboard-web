# empty value = use same-origin (nginx proxies /state, /router, /tasks, /events)
# a non-empty value here causes a remote HTTPS browser to fetch its OWN
# localhost (mixed-content blocked) and the live polling silently fails.
NEXT_PUBLIC_API_BASE=
